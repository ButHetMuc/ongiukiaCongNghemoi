<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.1.3/dist/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
    <title>Document</title>
</head>
<body>
    <div class="container">
    <table class="table">
        <thead>
          <tr>
            <th scope="col">id</th>
            <th scope="col">name</th>
            <th scope="col">quantity</th>
            <th scope="col">image</th>
            <th scope="col">option</th>
          </tr>
        </thead>
        <tbody>
            <% for(var key in mypaper) {%>
                <tr scope = "row">
                    <td> <%= mypaper[key].id %></td>
                    <td> <%= mypaper[key].name %></td>
                    <td> <%= mypaper[key].quantity %></td>
                    <td> <%= mypaper[key].image %></td>
                    <td>
                        <form action="/delete" method="post" enctype="multipart/form-data">
                            <input type="checkbox" name="id" hidden = "true" checked ="true" value="<%= mypaper[key].id %>" />
                            <button type="submit" class="btn btn-danger" >delete</button>
                        </form>
                    </td>
                </tr>
            <% } %>
        </tbody>
    </table>
    <div>
        <form action="/add" method="get">
            <button type="submit" class="btn btn-success" marginLeft = "auto">add new article</button>
        </form>
    </div>
</div>
</body>
</html>